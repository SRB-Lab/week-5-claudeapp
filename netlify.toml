[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Increase timeout for Azure AI agent calls (default 10s is too short)
  timeout = 30

[[plugins]]
  package = "@netlify/plugin-nextjs"
